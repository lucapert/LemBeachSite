{
  "name": "lem-beach-site",
  "description": "sito l'uomo e il mare",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "10.17.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    // questo viene utilizzato per la generazione della cartella out
    // per i siti statici
    // diamo poi npm run build
    // npm i -g serve se non lo abbiamo installato
    // serve della cartella out nell aporta 8000
    // serve -s out -p 8000
    // "build": "next build && next export"
    // a questo punto possiamo eliminare tutti
    // i file e tenere solo la folder out e tutto continua 
    // a funzionare
    // se deployamo in un sito questa folder è esclusa
    // dal gitignore
    "start": "next start -p $PORT"
  },
  "dependencies": {
    "next": "^10.0.0",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "sass": "^1.32.8",
    "scss": "^0.2.4"
  },
  "devDependencies": {
    "@types/node": "^14.14.34",
    "@types/react": "^17.0.3",
    "typescript": "^4.2.3"
  }
}
