module.exports = {
    i18n: {
        defaultLocale: 'it',
        locales: ['it', 'en'],
        // localeDetection: false // with this, next not detect the ACCEPT-LANGUAGE in header of the browser and then use the default. Otherwise it get 
        // the languge passed by the browser
    }
}