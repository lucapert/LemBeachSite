import React from 'react';

interface IProps {
    title: string;
    backGroudColor: string;
}

const TextRow = (props:IProps) => {
    return (
        <div>
            
        </div>
    );
};

export default TextRow;