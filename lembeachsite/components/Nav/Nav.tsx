import Link from 'next/link';
import Styles from './Nav.module.scss';
const Nav = () => {
    return (
        <nav className={Styles.nav}>
            <ul>
            </ul>   
        </nav>
    );
};

export default Nav;